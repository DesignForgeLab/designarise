/* All demo inquiries are handled locally by this mock HTTP endpoint. No storage or outbound delivery. Replace with a server provider when real delivery is configured. */
self.addEventListener('install',()=>self.skipWaiting());
self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));
self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==self.location.origin||u.pathname!=='/api/contact'||e.request.method!=='POST')return;e.respondWith((async()=>{try{const d=await e.request.json();const valid=typeof d.name==='string'&&d.name.trim()&&typeof d.email==='string'&&/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(d.email)&&d.projectType&&d.budget&&d.timeline&&typeof d.message==='string'&&d.message.trim().length>=10&&d.message.length<=5000;await new Promise(r=>setTimeout(r,650));return Response.json(valid?{ok:true,demo:true}:{ok:false,error:'Please complete all required fields with a valid email and at least 10 characters about your project.'},{status:valid?200:400});}catch{return Response.json({ok:false,error:'We could not read this inquiry. Please try again.'},{status:400});}})());});
